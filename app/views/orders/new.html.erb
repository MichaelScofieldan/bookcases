<h1>New Order</h1>
<div class="depot_form">
    <fieldset>
        <legend>Your information</legend>
        <%= render 'form' %>
    </fieldset>
</div>

<!-- <%= render 'form', order: @order %>  -->

<%= link_to 'Back', orders_path %>
